<!doctype html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#0f2942">
  <title>NAAP Parking System</title>
  <link rel="stylesheet" href="/styles.css" />
  <link rel="manifest" href="/manifest.json" />
  <script src="/offline-sync.js"></script>
  <script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/sw.js')
          .then(() => console.log('Service Worker registered'))
          .catch((err) => console.error('SW registration failed:', err));
      });
    }

    window.addEventListener('load', () => {
      if (window.OfflineManager) {
        OfflineManager.init((isOnline) => {
          const badge = document.getElementById('networkIndicator');
          const mobileBadge = document.getElementById('mobileNetworkIndicator');
          [badge, mobileBadge].forEach(el => {
            if (el) {
              el.textContent = isOnline ? '🟢 Online' : '🔴 Offline';
              if (!isOnline) {
                el.style.background = '#fef2f2';
                el.style.color = '#991b1b';
                el.style.borderColor = '#fecaca';
              } else {
                el.style.background = '';
                el.style.color = '';
                el.style.borderColor = '';
              }
            }
          });
        });
      }
    });
  </script>
  <!-- Dark Mode Init (Prevent FOUC) -->
  <script>
    (function () {
      const theme = localStorage.getItem('theme') || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      document.documentElement.setAttribute('data-theme', theme);
    })();
  </script>
</head>
<% const pathNow=typeof currentPath==="string" ? currentPath : "" ; const pageSlug=(pathNow==="/" ? "dashboard" :
  pathNow.replace(/\//g, "-" ).replace(/^-+/, "" )) || "dashboard" ; const pageTitleMap={ "/" : "Dashboard"
  , "/students" : "Students & Vehicles" , "/stickers" : "Stickers" , "/reports" : "Reports" , "/scanner"
  : "Gate Console" }; const pageTitle=pageTitleMap[pathNow] || "Verification" ; %>

  <body class="page-<%= pageSlug %>">
    <div class="app-shell">
      <!-- Top Navigation -->
      <nav class="top-nav">
        <div class="top-nav-upper">
          <div class="brand">
            <h1>NAAP Parking</h1>
          </div>
          <div class="top-nav-actions">
            <button class="theme-toggle" id="themeToggle" title="Toggle Mode" style="border:none;background:transparent;font-size:1.2rem;width:auto;height:auto;">🌓</button>
            <span class="chip chip-live desktop-only" id="networkIndicator" style="border-radius:999px;">🟢 Online</span>
            <a href="/logout" style="color:rgba(255,255,255,0.7); text-decoration:none; font-size:1.2rem; margin:0 0.5rem;" title="Sign Out">⎋</a>
            <div class="avatar-dot" style="width:32px; height:32px; font-size:0.8rem; box-shadow:none;">N</div>
          </div>
        </div>
        
        <div class="top-nav-links">
          <a href="/" class="<%= pathNow === '/' ? 'active' : '' %>">Dashboard</a>
          <a href="/students" class="<%= (pathNow === '/students' || pathNow === '/vehicles') ? 'active' : '' %>">Students &amp; Vehicles</a>
          <a href="/stickers" class="<%= pathNow === '/stickers' ? 'active' : '' %>">Stickers</a>
          <a href="/reports" class="<%= pathNow === '/reports' ? 'active' : '' %>">Reports</a>
          <a href="/scanner" class="<%= pathNow === '/scanner' ? 'active' : '' %>">Gate Console</a>
        </div>
      </nav>

      <div class="main-shell">

        <main class="container">

          <script>
            // Theme Toggle Logic
            window.addEventListener('DOMContentLoaded', () => {
              const toggleBtn = document.getElementById('themeToggle');
              const mobileToggleBtn = document.getElementById('mobileThemeToggle');

              const handleToggle = () => {
                const currentTheme = document.documentElement.getAttribute('data-theme');
                const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
                document.documentElement.setAttribute('data-theme', newTheme);
                localStorage.setItem('theme', newTheme);

                // Update button icons if needed
                [toggleBtn, mobileToggleBtn].forEach(btn => {
                  if (btn) btn.blur();
                });
              };

              if (toggleBtn) toggleBtn.addEventListener('click', handleToggle);
              if (mobileToggleBtn) mobileToggleBtn.addEventListener('click', handleToggle);
            });
          </script>